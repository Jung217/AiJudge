# AiJudge — 基隆地方法院毒品案件量刑預測模型

## 1. 專案目標

以司法院公開資料為基礎，建立可預測**臺灣基隆地方法院**毒品危害防制條例案件量刑結果的模型。模型須同時：

- 整合法定刑度約束（規則引擎），確保輸出不違反法律；
- 使用統計學習（XGBoost / LightGBM）反映實際量刑趨勢；
- 輸出可解釋的特徵影響力（SHAP）供決策參考；
- 明示本身為「統計參考」，**不得**作為判決依據。

## 2. 系統總覽

五層架構：

| 層 | 名稱 | 內容 |
|---|---|---|
| 1 | 資料層 | 司法院開放資料 + JSONL 快取 + 篩選結果 |
| 2 | 特徵層 | Regex + CKIP NLP + LLM 混合抽取 |
| 3 | 模型層 | 多任務分類 / 迴歸（行為、刑期、緩刑、折扣率） |
| 4 | 約束層 | 法定刑上下限、減刑規則、刑法§51 合併定刑 |
| 5 | 解釋與服務層 | SHAP 解釋 + FastAPI + 免責聲明 |

---

## 3. 第一階段：資料獲取與清理（3–4 週）

### 3.1 資料來源

- **主來源**：司法院開放資料平台，每月批次 **RAR** 下載（非 ZIP）
  - 下載 API：`https://opendata.judicial.gov.tw/api/FilesetLists/{fileSetId}/file`
  - 目錄 API：`https://opendata.judicial.gov.tw/api/Datasets?Keyword=裁判書&Page={n}`（回 JSON、無挑戰）
  - 初始 ID 範圍：**63694–64055**（約 362 個月度檔，對應 1996-01 ~ 2026-03）
  - 每個 fileSetId 對應一個月度 `{YYYYMM}裁判書.rar`，內含全國各法院 JSON 記錄
- **⚠ 下載端點狀態**（2026-04 實測）：
  - `/api/FilesetLists/{id}/file` 對**所有** fileSetId（含最新的 64055）皆回 500 `{"succeeded":false,"message":"Entity \"GetFilesetListFileQuery\" (N) was not found."}`
  - 即使用 Playwright + 真實 Chromium + 首頁 + dataset 頁 + TS cookie，結果相同
  - `/api/Datasets/{id}` 亦同。推測為**伺服器端 CQRS handler 目前失效**，非 client-side bot defense
  - 目錄 API `/api/Datasets?Keyword=裁判書` **正常**可枚舉所有 fileSetId
  - 建議：直接聯絡司法院資訊處 (<itsupport@judicial.gov.tw>) 或人工以瀏覽器下載 RAR
- **資料進件路徑**：使用者以瀏覽器下載月度 RAR 到 `data/raw/`；`records.iter_records_rar` 以 Windows 內建 bsdtar 解壓讀取
- **輔來源**：裁判書查詢系統（人工補當月未釋出資料）

### 3.2 記錄欄位

| 欄位 | 說明 |
|---|---|
| ID / JID | 裁判書唯一識別碼 |
| JYEAR | 年度（民國） |
| JCASE | 字別（訴、易、簡、聲、抗） |
| JNO | 號碼 |
| JDATE | 裁判日期 |
| JTITLE | 案由 |
| JFULL | 判決書全文 |
| JPDF | PDF 下載連結 |

### 3.3 篩選條件

1. **法院**：臺灣基隆地方法院（以 JFULL 首段比對，含「臺」「台」兩種寫法）
2. **案由**：含「毒品危害防制條例」或「毒品」
3. **程序**：一審有罪判決
4. **排除**：
   - 聲字（聲請觀察勒戒、聲請定應執行刑等非量刑事件）
   - 抗字（抗告，非一審）
   - 主文含「無罪」「不受理」「免訴」者
   - 協商判決（量刑為雙方合意，非純法官裁量）

### 3.4 清洗與段落拆解

判決書結構化為：
- **主文**：宣告刑、併科罰金、緩刑、易科罰金、沒收
- **犯罪事實**：涉及之毒品級別、行為、數量、時地
- **證據**：通常不入模型
- **理由**：
  - 論罪（引用條文）
  - **科刑**：量刑因子、減輕事由
  - 沒收
- **附表**：每筆交易明細（販賣案特重要）

### 3.5 修法版本標註

於每筆記錄加入 `law_version` 標籤：

- `<=2020-01-14`：舊法
- `2020-01-15 ~ 2023-05-29`：2020 修正版
- `>=2023-05-30`：2023 修正版

以**犯罪時**而非判決時判定。

---

## 4. 第二階段：特徵萃取與數據結構化（4–6 週）

### 4.1 標籤（多任務）

| 標籤 | 型別 | 說明 |
|---|---|---|
| y_sentence_months | int | 主刑有期徒刑月數 |
| y_fine_ntd | int | 併科罰金（新臺幣元） |
| y_probation | bool | 是否宣告緩刑 |
| y_probation_months | int | 緩刑期間（月），當 y_probation=1 時填 |
| y_convert_to_fine | bool | 是否得易科罰金（6 月以下） |
| y_aggregate_months | int | 定應執行刑（數罪併罰時） |
| y_confiscation_ntd | int | 沒收金額 |

### 4.2 客觀特徵

| 特徵 | 來源段落 | 備註 |
|---|---|---|
| 毒品級別 | 犯罪事實 | multi-hot（1–4 級） |
| 行為樣態 | 犯罪事實 / 主文 | 施用、持有、運輸、轉讓、製造、販賣、意圖販賣而持有 |
| 販賣次數 | 附表 | 每筆交易獨立 |
| **純質淨重（g）** | 犯罪事實 / 鑑定書段 | §4 加重之核心因子 |
| 交易金額 | 附表 | |
| 下游數量 | 附表 | 散布規模 |
| 共犯結構 | 犯罪事實 | 單獨 / 共同正犯 / 教唆 / 幫助 |
| 犯罪時 | 犯罪事實 | 與 law_version 連動 |

### 4.3 主觀特徵（法定減免）

| 特徵 | 法源 | 抽取方法 |
|---|---|---|
| **毒品條例§17Ⅰ**（供出來源因而查獲） | 強制減免 | 條文引用 + 「因而查獲」語境 |
| **毒品條例§17Ⅱ**（偵審均自白） | 強制減免 | 條文引用 + 「均自白」語境 |
| 累犯 | 刑法§47 | 條文引用（§47 + 釋憲後個案審酌語句） |
| 自首 | 刑法§62 | 條文引用 |
| 酌減其刑 | 刑法§59 | 條文引用 |
| 量刑因子（十款） | 刑法§57 | **LLM 抽取**：犯罪動機、手段、品行、智識、生活狀況、損害、犯後態度 |
| 和解 / 賠償 | — | LLM 抽取 |
| 戒癮狀態 | — | 施用案件限定 |

### 4.4 工具鏈

- 結構化欄位：regex
- 斷詞 / NER：CKIP Transformers
- 量刑因子：Claude API（批次 + prompt cache）
- **人工驗證**：先標註 200 筆作為 ground truth，計算抽取準確率

---

## 5. 第三階段：模型建構與權重設計（4–5 週）

### 5.1 樣本策略

基隆單院年案量約 300–500 件，5 年扣除排除項後可用樣本約 800–1,200 件，單院直訓練風險高。採：

**方案 A（推薦）：全國預訓練 + 基隆微調**
1. 以北部 5 地院（基、北、士、新北、桃）同期資料建立基礎模型
2. 加入「法院」作為特徵
3. 基隆樣本加權或 fine-tune

### 5.2 分層模型

```
 輸入特徵
   │
   ├─► 行為分類器 (LightGBM multi-class)
   │     → 販賣 / 施用 / 持有 / 轉讓 / ...
   │
   ├─► 刑期迴歸器 (XGBoost + Quantile Regression)
   │     → (p25, p50, p75) 月數
   │
   ├─► 緩刑分類器 (LightGBM binary)
   │     → P(緩刑 | 刑期 ≤ 2 年)
   │
   └─► 應執行刑折扣率迴歸器
         → 輸入：各宣告刑統計、罪數
         → 輸出：折扣率 ∈ [1/N, 1]
```

### 5.3 規則引擎（Hard Constraints）

每一次預測後強制校驗：

1. **法定刑上下限檢查**：依 (behavior, drug_level, law_version) 查表
2. **減刑規則**：§17Ⅰ、§17Ⅱ、§59 各自將上下限均乘以 1/2（刑法§66 減輕其刑「至二分之一」），按§70 遞減
3. **累犯加重**：刑法§47 — 僅加重**上限**（×1.5，不超過 30 年），下限不變。此設計為釋字 775 後之比例原則：若同時適用§17 減刑與累犯，強制提高下限將使兩制度衝突
4. **刑法§51 合併定刑**：`max(個刑) ≤ 合併刑 ≤ min(sum(個刑), 30 年)`

### 5.4 年份權重

- 以**犯罪時**而非判決時計算權重（避免修法造成標籤汙染）
- 2020-01-15、2023-05-30 修法邊界**重設**權重曲線，不跨邊界線性外推

---

## 6. 第四階段：回測驗證與誤差分析（2–3 週）

### 6.1 Walk-Forward Temporal CV

```
訓練期            驗證期
[----2020----]    [2021Q1]
[---2020+Q1--]    [2021Q2]
[--2020+Q1Q2-]    [2021Q3]
...滾動
```

**嚴禁** random split（會造成資料洩漏）。

### 6.2 評估指標

| 任務 | 指標 |
|---|---|
| 刑期迴歸 | MAE（月）、±3 月命中率、±6 月命中率 |
| 緩刑分類 | Precision / Recall / F1 |
| 易科罰金 | F1 |
| 折扣率 | R² |
| **法定刑越界率** | **必須 0%**（非 0 代表規則引擎失效） |

### 6.3 解釋性

- SHAP value 輸出各特徵對刑期的增減
- 誤差最大 10% 案件人工覆核，建立錯誤登記

---

## 7. 第五階段：部署與治理（2 週）

### 7.1 使用者場景

| 使用者 | 優先級 | 輸出形式 |
|---|---|---|
| 辯護律師 | 高 | 刑期區間 + 關鍵因子 |
| 學術研究 | 高 | 完整特徵重要性 |
| 被告 / 家屬 | 中 | 通俗化說明 + 免責聲明 |
| 法官 | **不支援** | 避免影響司法獨立 |

### 7.2 服務層

- FastAPI + 免責聲明
- 版本管理：資料、模型、規則引擎獨立版本
- Docker 部署

### 7.3 治理

- 偏誤定期檢測（disparate impact）
- 法官特徵**不納入**生產模型
- 重大修法後**強制重訓**

---

## 8. 技術堆疊

| 類別 | 選型 |
|---|---|
| 語言 | Python 3.10+ |
| 資料 | JSONL（初期）→ PostgreSQL（擴大後） |
| ML | XGBoost / LightGBM / scikit-learn |
| NLP | CKIP Transformers |
| LLM | Anthropic Claude API（量刑因子抽取） |
| 解釋 | SHAP |
| 服務 | FastAPI + Docker |
| 測試 | pytest |

---

## 9. 倫理與法律風險

| 風險 | 緩解 |
|---|---|
| 被誤用為法官購物工具 | 不揭露個別法官量刑風格 |
| 算法偏誤 | 定期檢測、維護偏誤登記 |
| 當事人期待失準 | 輸出明示為統計參考、非法律意見 |
| 個資法疑慮 | 訓練前去識別化（人名、身分證號） |
| 被用於判決 | 不提供 API 給法院內部系統 |

---

## 10. 專案結構

扁平結構（無套件層）：模組直接放於根目錄。

```
AiJudge/
├── plan.md                    # 本文件
├── README.md
├── LICENSE
├── requirements.txt
├── .gitignore
├── conftest.py                # pytest sys.path 設定
├── fetcher.py                 # 司法院開放資料下載
├── records.py                 # ZIP / JSON 解析 + Record dataclass
├── filter.py                  # 基隆 + 毒品案過濾
├── features.py                # 特徵抽取
├── rules.py                   # 法定刑規則引擎
├── models.py                  # ML 模型
├── scripts/                   # 執行入口
│   ├── 01_download.py         # 下載月度 ZIP
│   ├── 02_filter.py           # 篩出基隆毒品案
│   └── 03_explore.py          # 基礎統計
├── data/
│   ├── raw/                   # 原始 ZIP（.gitignore）
│   ├── filtered/              # 基隆毒品案 JSONL
│   └── processed/             # 特徵化後
└── tests/
    └── test_filter.py
```

**命名備註**：原規劃的 `parser.py` 改為 `records.py`，因 Python 3.9 內建 `parser` 模組（雖已棄用）仍會搶先載入造成 ImportError；模組主要 export 為 `Record` dataclass 與 `iter_records*`，`records` 名稱亦屬精確。

---

## 11. 里程碑

| 週次 | 產出 |
|---|---|
| W1–W2 | 下載完成、基隆毒品案 JSONL |
| W3–W4 | 判決書段落拆解器、100 筆人工標註 |
| W5–W8 | 特徵抽取器（含 LLM）、200 筆驗證 |
| W9–W12 | 基礎模型 + 規則引擎 |
| W13–W14 | Walk-forward 驗證、SHAP 分析 |
| W15–W16 | API + 倫理審查 |

---

## 12. 風險登記

| 風險 | 機率 | 影響 | 因應 |
|---|---|---|---|
| 基隆樣本不足 | 高 | 高 | 擴大至北部 5 院 |
| LLM 抽取成本高 | 中 | 中 | 批次 + prompt cache |
| 修法造成 drift | 高 | 中 | 犯罪時加權、版本分段 |
| 資料格式變更 | 低 | 高 | parser 抽象化 |
| 誤用倫理爭議 | 中 | 高 | 使用者範圍限縮 + 免責聲明 |

---

## 13. 立即可行步驟

0. ✅ `python scripts/00_demo.py` — pipeline 端到端（合成資料，通過）
1. ✅ **真實資料已驗證**（2026-01 + 2026-02 二個月 RAR）：
   - 解壓：Windows 內建 `bsdtar`（`C:\Windows\System32\tar.exe`）原生支援 RAR
   - 過濾：50 基隆毒品案（KL prefix 過濾，排除 Yunlin/Shilin 洩漏）
   - 特徵：100% 刑期覆蓋（43 有期徒刑 + 7 拘役），82 件行為偵測、58 件毒品級別偵測
2. ⏳ **資料回填**：使用者以瀏覽器手動下載 RAR 至 `data/raw/`（server API 500 未修復）
3. ⏳ 人工標註 100 筆驗證特徵抽取器（已有 `scripts/05_sample_for_labeling.py` + `06_evaluate_labels.py`）
4. ⏳ LLM (Claude API) 抽取 §57 量刑因子（scaffold 在 `scripts/07_llm_extract_factors.py`，待 API key + 標註資料）
5. ⏳ 擴大至北部 5 地院資料 → 建立基礎模型
