# Core data pipeline (phase 1)
requests>=2.31
tqdm>=4.66
pandas>=2.1
numpy>=1.26
pyarrow>=15.0   # parquet cache for build_dataframe

# ML (phase 3)
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3

# Explainability (phase 4)
shap>=0.44

# NLP (phase 2 — install when needed)
# ckip-transformers>=0.3.4
# §57 量刑因子改採啟發式 regex (scripts/art57_extract.py)，無外部 LLM API 依賴

# Serving (phase 5)
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0

# Dev
pytest>=8.0
