# Core data pipeline (phase 1)
requests>=2.31
tqdm>=4.66
pandas>=2.1
numpy>=1.26

# ML (phase 3)
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3

# Explainability (phase 4)
shap>=0.44

# NLP (phase 2 — install when needed)
# ckip-transformers>=0.3.4
# anthropic>=0.40  # for LLM-based §57 factor extraction

# Serving (phase 5)
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0

# Dev
pytest>=8.0
